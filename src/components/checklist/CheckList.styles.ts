import styled from "styled-components";

export const ChecklistItem = styled.li`
  display: inline-flex;
  width: 100%;
  align-items: center;
  gap: 0.8rem;
  margin-bottom: 1rem;
  position: relative;
  color: #cfcfcf;
  & svg {
    font-size: 1.2rem;
    color: #ff4b00;
  }
`;
